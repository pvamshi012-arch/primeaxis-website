[build]
builder = "nixpacks"

[build.nixpacks]
installCmd = "npm install && cd server && npm install"

[deploy]
startCommand = "node server/app.js"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
